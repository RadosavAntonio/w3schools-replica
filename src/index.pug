extend _component/layout/template

block cool-config
    -
        var myTitle = 'Learning to code HTML, CSS and JavaScript!';
        var myDescription = 'This is a HTML replica of w3schools!';
        var landingPage = 'Home';
        var activeLink = '';

block awesome-content
    h1 This is a replica of w3schools.com
    p I'll go through all possible HTML elements as well as CSS.
    h2 HTML
    p The language to build webpages. #[a(href=`${baseUrl}/html`) Check it out!]
    h2 CSS
    p the language for styling webpages.#[a(href=`${baseUrl}/css`) Change the look!]
    h2 JavaScript
    p The language to programming web pages.#[a(href=`${baseUrl}/javascript`) Make the magic!]
