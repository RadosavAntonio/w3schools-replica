extend ../../_component/layout/template

block cool-config
    -
        var myTitle = 'JavaScript variables!';
        var myDescription = 'JavaScript variables are containers for storing data values.';
        var landingPage = 'JavaScript';
        var activeLink = '/javascript/variables';
        var d = 8;
        var m = 4;
        var y = 1990;

block awesome-content
    h1=myTitle
    p=myDescription
    h2 Variables
    p Variables can be for exaple x, y and z. Each variable take a numeric or string value.

    h3 Here will have m=4, d=8 and y=1990.
    p Antonio birthday is: 
    birthday=d + "." + m + "." + y

    h3 Antonio's age in 2019 is
    age=2019-y